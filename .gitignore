.DS_Store
.env
.env.*
!.env.example
dist/
node_modules/
tmp/
*.tsbuildinfo
*.log

# Source/reference material that is not required by the website runtime.
# Anchored to the repository root: unanchored, "CSR/" also matched
# public/assets/community/csr/ (git's ignore matching is case-insensitive on
# macOS), which silently excluded nine images the homepage needs.
/CSR/
/LOGOS/
/Reference/
/SoniTransferBlog.docx
/audit-homepage/
/soni-transfer-website-deploy.zip
